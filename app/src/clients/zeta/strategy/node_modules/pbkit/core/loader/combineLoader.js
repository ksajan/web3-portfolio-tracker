"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function combineLoader(...loaders) {
    const _loaders = loaders.filter((l) => l);
    return {
        async load(path) {
            for (const loader of _loaders) {
                const loadResult = await loader.load(path);
                if (loadResult)
                    return loadResult;
            }
            return null;
        },
    };
}
exports.default = combineLoader;
