"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const index_1 = require("../visitor/index");
const printer_1 = require("./printer");
function minify(ast) {
    const printer = (0, printer_1.createPrinter)();
    const minifier = {
        ...index_1.visitor,
        visitFullIdent(visitor, node) {
            index_1.visitor.visitFullIdent(visitor, node);
            printer.print(" ");
        },
        visitType(visitor, node) {
            index_1.visitor.visitType(visitor, node);
            printer.print(" ");
        },
        visitKeyword(visitor, node) {
            index_1.visitor.visitKeyword(visitor, node);
            printer.print(" ");
        },
        visitComment() { },
        visitToken(_, node) {
            printer.print(node.text);
        },
    };
    minifier.visitProto(minifier, ast);
    return printer.done();
}
exports.default = minify;
