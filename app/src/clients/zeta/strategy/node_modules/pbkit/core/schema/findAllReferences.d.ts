import { Schema } from "./model";
import { ColRow } from "../parser/recursive-descent-parser";
import { Location } from "../parser/location";
export default function findAllReferences(schema: Schema, filePath: string, colRow: ColRow): Location[];
