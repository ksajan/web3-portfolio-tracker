import { Loader, LoadResult } from "./index";
export interface MemoizedLoader extends Loader {
    update(loadResult: LoadResult): void;
}
export default function memoizeLoader(l: Loader): MemoizedLoader;
